!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %title Pizzashop
    %meta{:content => "width=device-width,initial-scale=1", :name => "viewport"}/
    = csrf_meta_tags
    = csp_meta_tag
    = stylesheet_link_tag "application"
    = javascript_importmap_tags
  %body
    .container
      .row.justify-content-center
        .col-md-6.col-12
          %h1.display-2.text-center
            Welcome to George's Pizza!
      .row.justify-content-center#flashes
        .col-md-6.col-12
          -flash.each do |type, content|
            .alert.alert-dismissible.fade.show{class: "alert-#{flash_alert_css(type)}", role: :alert}
              %button.btn-close{type: :button,  aria: {label: "Close"}, data: {bs: {dismiss: :alert}}}
              =content.to_s.html_safe
      =yield

